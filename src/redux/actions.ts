const widgetGoLeft = () => (
    {
        type: 'widget/goLeft',
    }
);

const widgetGoRight = () => (
    {
        type: 'widget/goRight',
    }
);

export {
    widgetGoLeft,
    widgetGoRight
};
